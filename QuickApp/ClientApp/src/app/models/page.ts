export class Page {
  // The number of recods in the page
  pageSize: number = 0;

  // The total number of records
  totalRecords: number = 0;

  // The total number of pages
  totalPages: number = 0;

  // The current page number
  pageNumber: number = 0;
}
